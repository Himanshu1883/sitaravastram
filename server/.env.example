MONGODB_URI=just example 
# Optional at first deploy — API starts without DB; catalog routes need MONGODB_URI + seed
JWT_SECRET=change-me-in-production
PORT=5000
CLIENT_URL=http://localhost:5173
MOCK_OTP=123456
ADMIN_EMAIL=admin@sitaravastram.com
ADMIN_PASSWORD=sitara2026
# Optional: set a User.phone to role "admin" in MongoDB for OTP admin login via AuthModal
# Optional public URL for absolute media links (e.g. Cloudflare-proxied Railway domain)
# PUBLIC_API_URL=https://your-api.railway.app
